import styled from 'styled-components';

export const Container = styled.main`
    max-width: 1020px;
    margin: 0 auto;
    padding: 4rem 2rem;

    header{
        padding-bottom: 4rem;

        h1{
            margin-bottom: 2rem;
        }

        p{
            color: var(--gray-150);
        }
    }
`;